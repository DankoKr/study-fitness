@page
@model Website.Pages.MyPages.EditCardioPageModel
@{
}

<h1>Edit Cardio</h1>

@if (Model.Error.Length > 0)
{
    <div class="row mb-3">
        <div class="col-sm-9">
            <div class='alert alert-warning alert-dismissible fade show' role='alert'>
                <strong>@Model.Error</strong>
                <button type='button' class='btn-close' data-bs-dismiss='alert' aria-label='Close'></button>
            </div>
        </div>
    </div>
}

<h3>Current data</h3>
<p>Name: @Model.selectedC.Name</p>
<p>Difficulty: @Model.selectedC.Difficulty</p>
<p>Calories: @Model.selectedC.Calories</p>
<p>PictureURL: @Model.selectedC.PictureURL</p>


<form method="post">
    <input type="hidden" name="name" value="@Model.selectedC.Name" />
    <div class="row mb-3">
        <label class="col-sm-3 col-form-label">Difficulty</label>
        <div class="col-sm-6">
            <input type="text" class="form-control" name="difficulty" value="@Model.NewDifficulty" />
        </div>
    </div>
    <div class="row mb-3">
        <label class="col-sm-3 col-form-label">Calories</label>
        <div class="col-sm-6">
            <input type="text" class="form-control" name="calories" value="@Model.NewCalories" />
        </div>
    </div>
    <div class="row mb-3">
        <label class="col-sm-3 col-form-label">PictureURL</label>
        <div class="col-sm-6">
            <input type="text" class="form-control" name="picture" value="@Model.NewPictureURL" />
        </div>
    </div>

    @if (Model.Message.Length > 0)
    {
        <div class="row mb-3">
            <div class="col-sm-9">
                <div class='alert alert-success alert-dismissible fade show' role='alert'>
                    <strong>@Model.Message</strong>
                    <button type='button' class='btn-close' data-bs-dismiss='alert' aria-label='Close'></button>
                </div>
            </div>
        </div>
    }


    <div class="row mb-3">
        <div class="offset-sm-3 col-sm-3 d-grid">
            <button type="submit" class="btn btn-primary">Submit</button>
        </div>
        <div class="col-sm-3 d-grid">
            <a class="btn btn-outline-primary" href="/" role="button">Cancel</a>
        </div>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    }